[project]
name = "meta-model"
version = "0.1.0"
description = "Metadata-driven pipeline platform meta model primitives."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = [
  "asset",
  "base",
  "column",
  "container",
  "pipe",
]
